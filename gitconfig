[user]
	email = satgo1546@yeah.net
	name = Frog Chen
[push]
	default = simple
[credential]
	helper = cache --timeout=14400
[color]
	ui = true
[column]
	ui = auto
[alias]
	co = checkout
	com = checkout master
	cos = checkout satgo1546-dev
	cog = checkout gh-pages
	mg = merge
	mgm = merge master
	mgs = merge satgo1546-dev
	mgg = merge gh-pages
	br = branch
	ci = commit
	st = status -sb
	dup = clone --depth=1 --no-single-branch --recursive --shallow-submodules
	graph = log --all --graph --abbrev-commit --pretty=tformat:'%>|(15)%Cred%h %C(auto)%d%Creset %<|(66,trunc)%s %Cgreen%>(7)%ar %Cblue%an'
	undo-commit = reset HEAD~
	undo-merge = reset --hard ORIG_HEAD
	unstage = reset HEAD --
	last = log -1 HEAD
[core]
	editor = gvim -f -S ~/miscellaneous/scripts/popup-edit.vim
	excludesfile = /home/satgo1546/.gitignore_global
	eol = lf
	safecrlf = true
[merge]
	renormalize = true
[help]
	autoCorrect = 5
