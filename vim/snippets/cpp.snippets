extends c

snippet program
	#include <cstddef>
	#include <cstdio>

	int main(int argc, char* argv[]) {
		${0}
		return 0;
	}

snippet foreach
	for (${1:auto} ${2:i} : ${3:container}) {
		${0}
	}

snippet iterate
	for (auto ${1:i} = ${2:container}.begin(); $1 != $2.end(); ++$1) {
		${0}
	}
